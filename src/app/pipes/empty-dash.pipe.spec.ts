import { EmptyDashPipe } from './empty-dash.pipe';

describe('EmptyDashPipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyDashPipe();
    expect(pipe).toBeTruthy();
  });
});
